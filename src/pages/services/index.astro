---
import Button from '../../components/Button.astro';
import Grid from '../../layouts/Grid.astro';
import Layout from '../../layouts/Layout.astro';
const domain = 'michi.my';
---

<Layout title="Services">
    <Grid>
        <Button text="Collabora"    link=`https://cool.${domain}` />
        <Button text="Coturn"       link=`https://turn.${domain}` />
        <Button text="Element"      link=`https://element.${domain}` />
        <Button text="File Browser" link=`https://files.${domain}` />
        <Button text="Forgejo"      link=`https://git.${domain}` />
        <Button text="Immich"       link=`https://immich.${domain}` />
        <Button text="Invidious"    link=`https://iv.${domain}` />
        <Button text="Jellyfin"     link=`https://jelly.${domain}` />
        <Button text="Mail"         link=`https://mail.${domain}` />
        <Button text="Matrix"       link=`https://matrix.${domain}` />
        <Button text="Nextcloud"    link=`https://next.${domain}` />
        <Button text="Ollama"       link=`https://ai.${domain}` />
        <Button text="OnlyOffice"   link=`https://office.${domain}` />
        <Button text="OpenCloud"    link=`https://cloud.${domain}` />
        <Button text="PeerTube"     link=`https://pt.${domain}` />
        <Button text="Piped"        link=`https://pd.${domain}` />
        <Button text="Radicale"     link=`https://dav.${domain}` />
        <Button text="SearXNG"      link=`https://searx.${domain}` />
        <Button text="The Lounge"   link=`https://irc.${domain}` />
        <Button text="Uptime Kuma"  link=`https://uptime.${domain}` />
        <Button text="Vaultwarden"  link=`https://vault.${domain}` />
        <Button text="wastebin"     link=`https://bin.${domain}` />
        <Button text="Website"      link=`https://${domain}` />
        <Button text="websurfx"     link=`https://surfx.${domain}` />
        <Button text="Whoogle"      link=`https://whoogle.${domain}` />
        <Button text="Zipline"      link=`https://zip.${domain}` />
    </Grid>
</Layout>
